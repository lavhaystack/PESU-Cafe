import React from "react";
import CartItem from "./CartItem";

describe("CartItem API", () => {
    it("should render item name, quantity, and price", async () => {
        const mockData = [{itemName: "Veg-Grilled Sandwich", itemCount: 1, itemPrice}]
    })
})